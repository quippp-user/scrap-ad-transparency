First it'll open the page, You have to click on load more option (You don't need to add button for that as class name changes)
It'll scrap the link and save it in local machine
Then it'll run all the links at once and saved the images in provided folder path
